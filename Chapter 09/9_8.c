#include <stdio.h>
#define MAX(n1, n2) ((n1)>(n2)?(n1):(n2)) 
	// No space between MAX and Brackets
 int main () {			//Max between 20 and 10 is 20
 printf ("Max between 20 and 10 is %d\n", MAX (10, 20));
 return (0); 
}

